<div class="container mt-3">
    <h5>Total Orders in NLIMS Per Facility</h5>
    <div class="row">
        <div class="col-12">
            <table id="total_orders_table" class="table table-striped table-bordered" style="width:100%">
            <thead>
                <tr>
                    <th>Facility</th>
                    <th>Total Orders</th>
                </tr>
                </thead>
            </table>
        </div>
    </div>
</div>

<script>
$(document).ready(function () {
    let dataset = JSON.parse('<%=raw(@total_orders.to_json)%>');
    // let dataset = [
    //                 {'facility': 'Area 18 health centre', 'total_orders': 30},
    //                 {'facility': 'Area 25 health centre', 'total_orders': 50},
    //                 {'facility': 'Kabudula health centre', 'total_orders': 35},
    //                 {'facility': 'Kawale health centre', 'total_orders': 356},
    //                 {'facility': 'St Gabriel Hospital', 'total_orders': 307},
    //             ];
    
    $('#total_orders_table').DataTable( {
                data: dataset,
                columns: [
                    { data: 'facility' },
                    { data: 'total_orders' }
                ],
        } );
});
</script>